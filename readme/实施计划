Global Timezone Refactoring Plan
Centralize timezone handling across the entire Python codebase (backend and scripts) to use the dynamic setting from 
config/base.conf
 instead of hardcoded strings.

Proposed Changes
[Backend Configuration]
Create a centralized helper for timezone retrieval.

[MODIFY] 
config.py
Add import pytz.
Add 
get_timezone()
 helper to return a pytz object from SYSTEM.TIMEZONE.
[MODIFY] 
base.conf
Initialize SYSTEM.TIMEZONE if missing.
Update ADGUARD.TEMPLATE entries to match the global timezone.
[API Components]
[MODIFY] 
entertainment.py
Use config.get_timezone() for all time-based logic and DB entries.
[C++ Service Utilities]
[MODIFY] 
utils.cpp
: Ensure time retrieval matches user-defined timezone.
[SideMenu UI Enhancement]
[MODIFY] 
SideMenu.vue
: Add a live system clock below the user info that refractors dynamically.
[MODIFY] 
settings.py
: Ensure the settings API returns the current system time.
[System Timezone Persistence]
[MODIFY] 
config.py
: Add a map of human-readable timezones to OpenWrt UCI/TZ strings.
[MODIFY] 
settings.py
: When SYSTEM.TIMEZONE is updated, execute uci set system.@system[0].zonename and uci set system.@system[0].timezone, followed by uci commit system and potentially updating 
/etc/localtime
.
[License Activation]
[MODIFY] 
license_manager.cpp
: Add client_date to meta_info in 
activate()
.
[MODIFY] 
settings.py
: Add client_date to meta_info in 
activate_license()
 using dynamic timezone.
[Background Scripts]
Update all scripts to fetch the timezone from the centralized config.

[MODIFY] 
logger.py
Make EASTERN_TZ dynamic by loading it from backend.config.
[MODIFY] 
nft_manager.py
[MODIFY] 
log_monitor.py
[MODIFY] 
adguardhome.py
Replace all hardcoded "America/New_York" or "US/Eastern" with dynamic config retrieval.
Verification Plan
Automated Verification
Run a grep search to ensure no hardcoded timezone strings remain in 
.py
 or 
.conf
 files.
Run python3 scripts/nft_manager.py to verify no import errors.
Manual Verification
Change timezone in the Web UI (System Settings).
Check backend/logs/ and scripts/logs/ to verify timestamps shifted to the new zone.
离线宽限期功能实现计划
我们需要实现一个安全且防篡改的 30 天离线宽限期。该逻辑必须能够抵御用户的恶意操作，例如修改系统时钟或直接编辑离线状态 JSON 文件。

机制设计
触发条件：当 checkin() 签到失败（网络错误或服务器返回 500 等）并在之前并未开始倒计时时，系统开始计算宽限期。
安全状态存储：
将 GRACE_START_TIME（首次失败发生时的系统时间）和 LAST_CHECKIN_TIME（最后成功签到时间）保存为一个离线的 JSON 文件。
使用 HMAC-SHA256 对该 JSON 文件进行签名。HMAC 的密钥将根据硬件的 device_fingerprint() 动态生成，以防止将状态文件复制到其他设备或被手动篡改。
验证逻辑：
在允许离线运行服务之前，is_license_valid() 必须重新计算并验证 HMAC 签名。
如果签名匹配且 (当前时间 - GRACE_START_TIME) <= 30 天，则允许运行。
如果签名不匹配（表示发生篡改），则拒绝运行。
如果时间出现倒流（即当前时间 < GRACE_START_TIME - 允许的误差范围），则怀疑用户篡改了时钟，同样拒绝运行。
代码变动
[C++ Utils 工具类]
utils.hpp & utils.cpp:
添加 std::string hmac_sha256(const std::string& key, const std::string& data) 函数。
链接 mbedtls 或 openssl 库（该项目中已经在 Makefile 中链接了 -lcrypto 进行构建）。
[C++ LicenseManager 授权管理]
license_manager.cpp:
修改 update_local_license() 方法使其能够写入包含 JSON 字符串及其 HMAC 签名的本地配置文件。
修改 is_license_valid() 方法，执行上述的校验逻辑。
修复 get_grace_period_days() 方法，将其倒数逻辑的基础由 LAST_CHECKIN_TIME 改为真实的宽限期起点 GRACE_START_TIME。